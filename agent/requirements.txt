# VoiceOps LiveKit voice agent
livekit-agents[openai,silero]>=1.0.0
psycopg[binary]>=3.2
python-dotenv>=1.0
# Add real STT/TTS providers as needed, e.g.:
# livekit-plugins-deepgram
# livekit-plugins-cartesia
# livekit-plugins-elevenlabs
