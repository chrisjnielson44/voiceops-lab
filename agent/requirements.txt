# VoiceOps LiveKit voice agent
# ElevenLabs handles STT (Scribe v2 realtime) + TTS from one key; LLM = local MLX.
livekit-agents[openai,silero,elevenlabs]>=1.0.0
psycopg[binary]>=3.2
python-dotenv>=1.0
