# LiveKit voice agent — copy to .env and fill in for real audio/telephony.

# LiveKit Cloud (from `lk cloud auth` / project settings)
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_AGENT_NAME=voiceops-agent

# Local model (shared with the web app)
LOCAL_LLM_BASE_URL=http://127.0.0.1:8080/v1
LOCAL_LLM_MODEL=mlx-community/Qwen2.5-7B-Instruct-4bit
LOCAL_LLM_API_KEY=mlx-local

# Same Neon DB the web app uses (ground-truth tools)
DATABASE_URL_UNPOOLED=

# Speech: ElevenLabs handles BOTH STT (Scribe v2 realtime) and TTS from one key.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=        # optional; defaults to the plugin's default voice

# Cockpit bridge — where to forward live turns/tools/lifecycle so the web app's
# context-graph / prediction / reasoning panels track the call in real time.
# Point at the backend (POST /api/agent/ingest). Leave blank to disable.
BACKEND_URL=http://127.0.0.1:8000
# Must match the backend's BACKEND_INTERNAL_TOKEN when that shared secret is set.
BACKEND_INTERNAL_TOKEN=
