{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/auth/:path*", "destination": "https://voiceops-auth.fly.dev/api/auth/:path*" },
    { "source": "/api/:path*", "destination": "https://voiceops-api.fly.dev/api/:path*" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
